machine:
  node:
    version: 6.11.0

dependencies:
  pre:
    - rm -rf ./node_modules

deployment:
  development:
    branch: master
    commands:
      - npm install --only=dev
      - npm run build
    heroku:
      appname: yenarue-utils
